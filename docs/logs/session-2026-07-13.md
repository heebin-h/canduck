# Session Log — 2026-07-13

> 주의: 이 세션의 turn-8~19는 날짜 인지 오류로 `session-2026-07-12.md`에 이어 기록됨 (실제로는 12→13일에 걸친 세션). turn-20부터 이 파일.

---

## [turn-20] heebin → Claude

(셀프 리뷰 지적사항) 적용 ㄱ

## [turn-20] Claude → heebin

- 리뷰 지적 전건 적용:
  - **H1**: `poses_abort()` 신설 + STOP 경로에서 시퀀스 선중단 (comm.cpp/poses.h/poses.cpp)
  - **M1**: ENABLE 0 시 `motor_stop()` (OE#는 서보만 게이트)
  - **M2**: uart_smoke에 `flush()` — BOOT 배너 확인 직후 잔여 이벤트(dry-run ERR) 제거로 검증 오염 방지
  - **M3**: vbat = `analogReadMilliVolts()*3` (eFuse 캘리브레이션)
  - **M4**: daemon.stop()을 try/except/finally 구조로 — UART 장애에도 종료 완주, 종료 시 STOP도 송신
  - **L4**: display_tk `_available` 데드 필드 제거
  - **README 2종** 동기화 (RPi4/tkinter/신규 모듈/구현 상태/canduck-smoke, M5 과대주장 정정 포함: "노트북 데몬 완주는 DevKit USB 연결 필요" 명시)
- **회귀 체크가 신규 버그 1건 추가 발견·수정**: macOS AppKit은 NSWindow를 메인 스레드로 강제 → 워커 스레드 Tk가 프로세스 크래시 (NSInternalInconsistencyException, 실측). darwin 가드 추가 — macOS는 표시 생략(로그 경고), RPi/Linux만 창 표시. ponytail 주석으로 업그레이드 경로(서브프로세스 뷰어) 명시.
- 검증: py_compile 전체 + venv 회귀(표정 9종 렌더, darwin no-op 표시 경로, gpio mock) 통과. 펌웨어 컴파일은 여전히 heebin 로컬 pio 필요.
- 로그 파일 날짜 정정: turn-20부터 session-2026-07-13.md (그간 12일 파일에 오기록).

- **Tools used**: Bash(python), Edit, Read
- **Files changed**: firmware 4 (poses.h/poses.cpp/comm.cpp/vbat.h), host 5 (uart_smoke/daemon/display_tk/README/pyproject아님—README만), firmware/README.md
- **Decisions/Next**: 낮음 3건(L1~L3)은 미적용 유지 (에러코드 표 개정 필요 — W3에서 architecture.md와 함께). heebin — push/merge, pio run 컴파일 확인.
